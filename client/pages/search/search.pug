search(
  placeholder="输入菜名"
  bind:confirm="handleSearch"
)

wrapper(
  loading="{{loading}}"
  errorData="{{errorData}}"
  bind:btntap="handleSearch"
)
  view.black(slot="page")
    view.menu-list
      menuCard(
        wx:for="{{menuList}}"
        wx:key="_id",
        wx:for-item="menu"
        menu="{{menu}}"
      )